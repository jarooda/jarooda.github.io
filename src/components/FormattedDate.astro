---
interface Props {
  lang?: string
	date: Date;
}

const { date, lang } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(lang || 'id-id', {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
