---
import AnimatedButton from './AnimatedButton.astro'

interface Props {
  href: string
  rel?: string
  target?: string
  icon?: string
  block?: boolean
  isContrast?: boolean
  test?: string
}

const { href, rel, target, icon, block, isContrast, test } = Astro.props;
---

<AnimatedButton
  href={href}
  rel={rel}
  target={target}
  iconName={icon}
  block={block}
  isContrast={true}
  dataTest={test}
  as="a"
>
  <slot />
</AnimatedButton>